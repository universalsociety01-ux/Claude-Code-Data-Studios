# Local Settings Template

Copy to .claude/settings.local.json and customize.

## Common Customizations
- Add tool permissions: "allow": ["Bash(docker *)"]
- Disable hooks: remove entry from trigger section
- Add custom hooks: new script + settings entry
- Adjust timeouts per hook
